import React from 'react'
import { Route, Switch, Redirect } from 'react-router-dom';

const Nav = (props) => {
    console.log(props.history);

    return (
        <div>
            NAV LINKS HERE
        </div>
    )
}

export default Nav
