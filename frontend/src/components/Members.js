import React from 'react';

const Members = (props) => {
    //props.name, props.image_url,country, email, age, 

    return (
        <>
            <h1>{props.name}</h1>
            <div></div>
        </>
    )
}

export default Members;